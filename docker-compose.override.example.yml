# Optional: copy this file to docker-compose.override.yml to edit the bundled
# hook configuration locally without rebuilding the image.
services:
  openclash:
    volumes:
      - ${OPENCLASH_HOOK_CONFIG_DIR:-./hooks/config}:/usr/local/share/openclash-hooks/config:ro
